import {DefaultTheme} from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#FF0000',
        secondary: '#00FF00',
        error: '#0000ff',
        success: '#FF0000'
    }
};
